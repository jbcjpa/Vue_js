let counter = document.getElementById("counter").innerHTML

function add() {
    let new_counter = parseInt(counter);
    new_counter++;
    counter = new_counter;
    console.log(counter);
}

function add() {
    let new_counter = parseInt(counter);
    new_counter++;
    counter = new_counter;
    console.log(counter);
}

function add() {
    let new_counter = parseInt(counter);
    new_counter++;
    counter = new_counter;
    console.log(counter);
}

function add() {
    let new_counter = parseInt(counter);
    new_counter++;
    counter = new_counter;
    console.log(counter);
}